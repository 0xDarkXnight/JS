const accountId = 1443
let accountEmail = "hue@hue.com"
accountName = "hue" //aise bhi kar sakte variable ko declare
//dont use var because of issue in block scope and functinal scope. 
console.log(accountId);
console.table([accountEmail, accountId, accountName]);
